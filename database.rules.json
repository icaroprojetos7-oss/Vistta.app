{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid && ((!data.exists() && newData.child('role').val() == 'admin' && !newData.child('empresaId').exists()) || (!data.exists() && newData.child('empresaId').exists() && (newData.child('role').val() == 'admin' || newData.child('role').val() == 'vendedor') && newData.child('convidadoPor').exists() && root.child('users').child(newData.child('convidadoPor').val()).child('role').val() == 'admin' && root.child('users').child(newData.child('convidadoPor').val()).child('empresaId').val() == newData.child('empresaId').val()) || (data.exists() && !data.child('empresaId').exists() && newData.child('empresaId').exists() && newData.child('role').val() == data.child('role').val() && root.child('empresas').child(newData.child('empresaId').val()).child('info').child('criadoPor').val() == auth.uid) || (data.exists() && newData.child('role').val() == data.child('role').val() && newData.child('empresaId').val() == data.child('empresaId').val()))",
        ".validate": "newData.hasChildren(['role']) && (newData.child('role').val() == 'admin' || newData.child('role').val() == 'vendedor')"
      }
    },
    "empresas": {
      "$empresaId": {
        "info": {
          ".read": "auth != null && root.child('users').child(auth.uid).child('empresaId').val() == $empresaId",
          ".write": "auth != null && ((!data.exists() && newData.child('criadoPor').val() == auth.uid) || (root.child('users').child(auth.uid).child('empresaId').val() == $empresaId && root.child('users').child(auth.uid).child('role').val() == 'admin'))",
          ".validate": "newData.hasChildren(['nome', 'criadoPor']) && newData.child('nome').isString() && newData.child('criadoPor').isString()"
        },
        "$collection": {
          ".read": "auth != null && root.child('users').child(auth.uid).child('empresaId').val() == $empresaId",
          ".indexOn": ["data", "status", "codigo", "cliId", "clienteId", "fornecedorId"],
          "$recordId": {
            ".write": "auth != null && root.child('users').child(auth.uid).child('empresaId').val() == $empresaId && (root.child('users').child(auth.uid).child('role').val() == 'admin' || (($collection == 'produtos' || $collection == 'clientes' || $collection == 'categorias') && newData.exists()) || (($collection == 'orcamentos' || $collection == 'ordensServico') && newData.exists()))",
            ".validate": "($collection != 'produtos' || (newData.hasChildren(['codigo', 'marca', 'custo', 'venda', 'qtd', 'min']) && newData.child('custo').isNumber() && newData.child('custo').val() >= 0 && newData.child('venda').isNumber() && newData.child('venda').val() >= 0 && newData.child('qtd').isNumber() && newData.child('qtd').val() >= 0 && newData.child('min').isNumber() && newData.child('min').val() >= 0)) && ($collection != 'vendas' || (newData.hasChildren(['total', 'subtotal', 'desconto', 'data', 'caixaId', 'itensDetalhados']) && newData.child('total').isNumber() && newData.child('total').val() >= 0 && newData.child('subtotal').isNumber() && newData.child('desconto').isNumber() && newData.child('desconto').val() >= 0)) && ($collection != 'contas' || (newData.hasChildren(['descricao', 'tipo', 'valor']) && newData.child('valor').isNumber() && newData.child('valor').val() >= 0))",
            "lancamentos": {
              "$lancamentoId": {
                ".write": "auth != null && root.child('users').child(auth.uid).child('empresaId').val() == $empresaId && root.child('users').child(auth.uid).child('role').val() == 'admin' && data.parent().parent().child('status').val() == 'aberto'",
                ".validate": "newData.hasChildren(['tipo', 'descricao', 'valor', 'data']) && (newData.child('tipo').val() == 'entrada' || newData.child('tipo').val() == 'saida' || newData.child('tipo').val() == 'sangria') && newData.child('valor').isNumber() && newData.child('valor').val() > 0"
              }
            }
          }
        }
      }
    }
  }
}